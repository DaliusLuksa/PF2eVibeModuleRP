# Machine-local LevelDB caches of the compendium packs - Foundry rebuilds
# them from packs/*.db on first launch. Never commit them.
packs/*/

# Release zip built by build-module.bat - never commit it.
/module.zip
