# Machine-local LevelDB cache of the compendium pack - Foundry rebuilds it
# from packs/*.db on first launch. Never commit it.
packs/shielded-arm-effect/

# Release zip built by build-module.bat - never commit it.
/module.zip
